/** Thin re-export so admin lib can import profile/role helpers locally. */
export {
  parseRole,
  headlineName,
  composeUsAddress,
  parseApprovalStatus,
  isUserApproved,
  type UserRole,
  type ApprovalStatus,
} from "@pulse/shared";
