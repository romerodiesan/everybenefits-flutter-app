{
  "name": "every-benefits-functions",
  "description": "Trusted callables for votes and role changes",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@pulse/shared": "file:../packages/pulse-shared",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "private": true
}
