# Allow ignored dependency build scripts listed in pnpm-workspace onlyBuiltDependencies
# after explicit approve. Until then, skip failing the install.
# Kept at repo root so Firebase App Hosting / Turborepo installs see the same pnpm policy.
strict-dep-builds=false
confirmModulesPurge=false
