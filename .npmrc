# pnpm project settings live in pnpm-workspace.yaml (strictDepBuilds,
# confirmModulesPurge, allowBuilds, overrides). Keep this file free of
# pnpm-only keys so `npx` / npm do not warn about unknown project config.
